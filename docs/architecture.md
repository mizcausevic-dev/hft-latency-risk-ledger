# Architecture

The repo has three intentionally separate layers:

- Rust scores raw latency evidence into deterministic venue findings.
- Python packages those findings into a diligence narrative for executives and investors.
- TypeScript renders the same model into a static operator surface.

The fixture is synthetic. Production use should replace it with sanitized telemetry exports and keep raw trading evidence outside the public repository.
